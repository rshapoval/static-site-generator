import { SectionProps } from "@/interfaces/section";

export default function FooterSection({ data, setData }: SectionProps) {
  return (
    <div className="mt-5">
      <h2 className="mb-4 text-2xl">Footer section</h2>
    </div>
  );
}
